# URL base da API backend.
# ATENÇÃO: o Vite resolve esta variável no BUILD, não em runtime — para publicar
# em outro host é preciso rebuildar o frontend com o valor correto.
# Também é a origem usada pelo WebSocket de permissões (http -> ws, https -> wss).
VITE_API_BASE_URL=http://localhost:8080
